/**
 * The brand name, in one place.
 *
 * It was written out by hand in the header, the admin console, the PayPal
 * sheet and the signup copy, and the stories credited "the Seoul lab" — a
 * placeholder that read like a different company's byline on our own content.
 * A name that appears in four languages and on someone else's payment screen
 * is not a string to retype; renaming the store should be one edit.
 *
 * `WORDMARK` is the letterspaced display form used as a logo. `NAME` is the
 * same name set in running text, where all-caps would shout.
 */

export const WORDMARK = 'SKINVERSE'
export const NAME = 'Skinverse'
